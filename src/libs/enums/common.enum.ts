export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}

export enum FileType {
  IMAGE = 'IMAGE',
  AUDIO = 'AUDIO',
  DOCUMENT = 'DOCUMENT',
}

export enum Currency {
  UZS = 'UZS',
  USD = 'USD',
}
